{
  "name": "quolance",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "prepare": "husky install",
    "lint": "npm run lint:ui",
    "lint:ui": "cd quolance-ui && npm run lint",
    "lint:backend": "cd backend && ./mvnw checkstyle:check"
  },
  "devDependencies": {
    "@commitlint/cli": "^16.3.0",
    "@commitlint/config-conventional": "^16.2.4",
    "husky": "^7.0.4"
  }
}